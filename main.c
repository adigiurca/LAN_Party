#include "functions.h"

int main() {
    NODE *head;
    head = NULL;
    char* teams;

    //Testing teams file content
//    char c = getc(teams);
//    while (c != EOF){
//        putchar(c);
//        c = getc(teams);
//    }

    //read_names_from_file_and_add_at_beginning(&teams, head);

}

